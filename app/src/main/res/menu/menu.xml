<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:balloonberry="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/settings"
          android:title="Settings" >
        <!-- "file" submenu -->
        <menu>

            <item android:id="@+id/clean"
                android:title="Disconnect"
                balloonberry:showAsAction="always"/>

            <item android:id="@+id/Wifi_direct_discover"
                android:title="Discovery"
                balloonberry:showAsAction="ifRoom|withText"/>

            <item android:id="@+id/Wifi_direct_enable"
                android:title=" WiFi Direct ON/OFF"
                balloonberry:showAsAction="ifRoom|withText"/>

            
<!--             <item android:id="@+id/connect" -->
<!--                   android:title="Connect to Socket" /> -->
        </menu>
    </item>
</menu>

